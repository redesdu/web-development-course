name: Deploy to GitHub Pages

on:
  workflow_dispatch:
    inputs:
      publish_blocked_candidate:
        description: Publish even when course/RELEASE.md records unresolved review items
        required: true
        type: boolean
        default: false

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run ci
      - name: Check release readiness
        if: ${{ !inputs.publish_blocked_candidate }}
        run: npm run check:release
      - name: Record explicit release-gate override
        if: ${{ inputs.publish_blocked_candidate }}
        run: echo "::warning::Publishing an explicitly authorized candidate while course/RELEASE.md remains blocked."
      - uses: actions/upload-artifact@v5
        if: ${{ failure() }}
        with:
          name: failed-release-evidence-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 14
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
